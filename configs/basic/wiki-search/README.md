# wiki-search (2 GPU)

Multi-turn tool calling (search a wiki corpus via native V1 tools), LoRA on `Qwen3-4B`. Dev mirror
of [`examples/basic/wiki-search`](../../../examples/basic/wiki-search).

```bash
uv run rl @ configs/basic/wiki-search/rl.toml
```
